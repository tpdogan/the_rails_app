<p id="notice"><%= notice %></p>

<h1>Cars</h1>

<table>
  <thead>
    <tr>
      <th>Make</th>
      <th>Model</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
<%# ERB is like PHP, code embedded inside HTML 
use <% to runs some code
use <%= to return some output to HTML
use <%# to comment out %>
<%# See below example for a clear code%>

<%# raw means run the code as in html %>
  <%= raw "<h2>The car list</h2>" %>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.make %></td>
        <td><%= car.model %></td>
        <td><%= car.year %></td>
        <td><%= link_to 'Show', car %></td>
        <td><%= link_to 'Edit', edit_car_path(car) %></td>
        <td><%= link_to 'Destroy', car, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
<%# A simple way to render all cars without much coding is via partials
<P= render @cars P> replace P with %
and involve <td>...</td> parts in a partial
Basically render same thing to all elements, rails got this..
%>
  </tbody>
</table>

<br>

<%= link_to 'New Car', new_car_path %>
